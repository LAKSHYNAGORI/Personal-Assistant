import pyttsx3
import webbrowser as  Webbrowser
import pyaudio
import speech_recognition as sr
import datetime
import wikipedia
import os
import sys




engine = pyttsx3.init('sapi5')
voices = engine.getProperty('voices')
# print(voices[1]).id
engine.setProperty('voices',voices[1].id)


def speak(audio):
    engine.say(audio)
    engine.runAndWait()
def wishme(datetime):
    hour = int(datetime.datetime.now().hour)
    if hour>0 and hour<12:
        speak("good morning")
    elif hour>12 and hour<18:
        speak('good afternon')
    else:
        speak("good evening")
    speak("hey lakshy, jarvis here please let know how can i help you")
def takecommand():
   # it take a mirophone input from the user and return string output
   r = sr.Recognizer()
   with sr.Microphone() as source:
       print('listning.......')
       r.pause_threshold = 1
       audio = r.listen(source)
   try:
        print('Recognizing....')
        query = r.recognize_google(audio,language="en-in")
        print('user said',query)
   except Exception as e:
        print(e)
        speak("say that again please.......")
        return "none"
   return query
if __name__=='__main__':
    wishme(datetime)
    if 1:
        query = takecommand().lower()
        
        if 'wikipedia' in query:
            speak ("searching wikipedia.....please wait for a while")
            query = query.replace("wikipedia","")
            results = Wikipedia.summary(query,setence=2)
            speak = ("according to wikipedia")
            print(results)
            # speak (results)''''''''''''
        elif 'open youtube' in query:
            Webbrowser.open("youtube.com")
        elif'open google' in query:
            Webbrowser.open("google.com")
        elif 'open notpad' in query:
            npath = 'c:\\windows\system\systems32\notpad.exe'
        elif 'open command prompt' in query:
            os.system('start cmd')
        elif 'open stackoverFlow' in query:
            Webbrowser.open("calender.com")
        elif 'open calender' in query:
            Webbrowser.open("calender.com")
        elif 'open code' in query:
            codepath = "D:\personal assistant"
            os.startfile(codepath)
        elif 'time' in query:
            strTime = datetime.datetime.now().strftime("%h:%m:%s")
            speak(f"maam the time is {strTime}")
        elif 'no thanks' in query:
            speak('thankyou u maam for using me. have a good day')
sys.exit()
            
